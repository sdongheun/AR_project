# AR Map (가칭) MVP 정리

## 문서 목적

- 이 문서는 2026 한국관광공사 공모전용 `AR Map` 프로젝트의 초기 MVP 구상을 정리한 초안이다.
- 현재 내용은 확정본이 아니라, 이후 대화에서 계속 수정 및 구체화할 기준 문서로 사용한다.

## 프로젝트 개요

- 프로젝트명: `AR Map` (가칭)
- 프로젝트 성격: 2026 한국관광공사 공모전 프로젝트
- 해결하려는 문제:
  - 2D 지도로는 여행자가 실제 눈앞 풍경과 지도 정보를 직관적으로 매칭하기 어렵다.
  - 낯선 도심이나 관광지에서 눈앞 건물 또는 장소가 무엇인지 즉시 알기 어렵다.
- 목표:
  - 사용자가 카메라로 바라보는 건물 또는 관광지에 대해, 해당 위치에 맞는 이름 정보를 AR 형태로 보여준다.

## 타깃 사용자

- 낯선 도심이나 관광지에서 눈앞 장소의 정체를 빠르게 알고 싶은 사용자
- 특히 `건물 이름` 또는 `관광지 이름`을 시각적으로 확인하고 싶은 여행자

## 현재 기준 핵심 사용자 시나리오

1. 사용자가 앱을 실행한다.
2. 첫 화면은 카메라 기반 화면이다.
3. 사용자는 궁금한 건물 또는 관광지를 카메라로 비춘다.
4. 시스템은 사용자의 위치, 방향, 기기 센서를 바탕으로 대상 위치를 식별한다.
5. 대상 위치에 AR 마커 또는 텍스트를 띄운다.
6. 건물이면 건물명, 관광지면 관광지명을 표시한다.

## 현재 정의된 MVP 목표

- 사용자가 카메라로 바라보는 대상이 `건물` 또는 `관광지`일 때,
- 해당 위치를 가리키는 `정확한 마커`를 표시하고,
- 그 마커에 대응하는 `건물명` 또는 `관광지명`을 AR 텍스트로 보여주는 것

## MVP 표시 정보

### 건물

- 건물명
- 예시: `신세계`

### 관광지

- 관광지명
- 예시: `OO해변`, `OO거리`

## 현재 생각 중인 인식 범위

- 1차적으로는 `한 번에 하나의 대상만` 안정적으로 인식 및 표시하는 것을 목표로 한다.
- 이후 확장 방향:
  1. 한 화면 내 여러 대상 표시(보류)
  2. 사용자가 좌우로 방향을 돌릴 때 다른 대상 정보 존재 여부 안내

## 데이터 및 위치 판단 방식

### 건물

- `브이월드 3D 데이터`를 활용해 정확도를 높이는 방향을 고려 중이다.
- 목표는 실제 건물 위치에 최대한 가깝게 텍스트를 띄우는 것이다.

### 관광지

- 단순 관광지 장소는 `GPS 기반 지도 API`를 활용하는 방향을 고려 중이다.

### 센서 활용

- GPS 사용 예정
- 나침반 사용 예정
- 자이로센서 사용 예정

## 현재 서비스 범위 구상

- 아직 `모든 건물`을 대상으로 할지,
- 아니면 `특정 지역의 관광지 중심`으로 제한할지는 미정이다.

## 지역 확장 계획

- 우선 현재 위치 주변의 `특정 건물`을 타깃으로 기능이 정상 동작하는지 검증한다.
- 검증 후 부산 지역으로 확장하는 것을 고려한다.

## 플랫폼 우선순위

- 1차 개발 우선순위: `iPhone`
- 개발 환경 배경: 현재 사용자는 iPhone / Mac 환경
- 이후 여건이 되면 크로스플랫폼(android, ios) 확장 검토

## 단계별 구현 우선순위

1. 먼저 건물에 해당 마커를 띄울 수 있는지 검증 [ ]
2. 해당 건물 정보를 받아올 수 있는지 연결 [ ]
3. 이후 길찾기 기능 구현 검토 [ ]

## 현재 확정된 점

- 첫 화면은 카메라 기반 화면이다.
- MVP의 첫 목표는 상세 정보가 아니라 `건물명/관광지명 표시`이다.
- 정확한 장소 지정을 위한 `마커 + 이름 텍스트` 구조를 우선한다.
- 초기에 한 번에 하나의 대상만 다룬다.
- iPhone 우선 개발로 시작한다.
- 위치 판별에는 GPS, 나침반, 자이로센서를 활용한다.

## 아직 미확정인 점

- 타깃 범위를 `모든 건물`로 할지 `관광지 중심`으로 할지
- 건물과 관광지 중 어느 쪽을 MVP의 주 타깃으로 삼을지
- 건물/관광지 판별 로직을 어떤 기준으로 나눌지
- 표시 정확도를 어디까지 성공 기준으로 볼지
- 브이월드 3D 데이터와 지도 API를 실제로 어떤 방식으로 결합할지
- AR 마커 표현 방식이 `텍스트 중심`인지 `아이콘+텍스트`인지
- 길찾기를 MVP에 포함할지, 후속 기능으로 분리할지

## 현재 문장으로 정리한 MVP 정의

> 사용자가 iPhone 카메라로 바라보는 건물 또는 관광지의 실제 위치에 AR 마커를 띄우고, 해당 대상의 이름을 화면에 표시하는 기능을 우선 구현한다.

## 다음 대화에서 우선 확정해야 할 항목

- MVP의 1차 타깃은 `건물`인지 `관광지`인지
- 시연 지역은 어디인지
- 이름 외에 함께 보여줄 최소 부가정보가 있는지
- 성공 판정 기준을 어떻게 둘 것인지
- 실제 데이터 소스를 어떤 조합으로 쓸 것인지

## 변경 이력

- 2026-05-01: 사용자 1차 답변 기준 초안 작성
